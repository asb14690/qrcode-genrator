{
  "name": "qrcode-genrator",
  "version": "1.0.0",
  "description": "Package convert an URL into a QR codes image",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "qr-image": "^3.2.0"
  }
}
